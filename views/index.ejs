<!DOCTYPE html>
<html lang="bg">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1.0"/>
  <title>TumnataMreja – Market</title>
  <link rel="stylesheet" href="/css/style.css" />
  <link rel="icon" href="data:image/svg+xml,…(твоя data-URI)" />
</head>
<body>
  <!-- MATRIX BACKGROUND -->
  <div class="matrix-bg" id="matrixBg"></div>

  <!-- HEADER -->
  <header class="site-header">
    <nav class="header-nav container">
      <a href="/" class="logo-link">
        <img src="https://i.postimg.cc/nL66r5Kh/logo.png" alt="logo" class="logo-img glitch"/>
      </a>

      <ul class="nav-list">
        <li><a href="/">НАЧАЛО</a></li>
        <% if (user) { %>
          <li><a href="/dashboard">СИСТЕМА</a></li>
          <li><a href="/logout">ИЗХОД</a></li>
        <% } else { %>
          <li><a href="/login">LOGIN</a></li>
          <li><a href="/register">REGISTER</a></li>
        <% } %>
      </ul>
    </nav>
  </header>

  <!-- MAIN CONTENT -->
  <main class="container main-content">
    <% if (!user) { %>
      <section class="auth-prompt">
        <h1>⚠️ РЕГИСТРИРАЙ СЕ – NO KYC ⚠️</h1>
        <p>Трябва да си регистриран, за да разглеждаш и купуваш.</p>
        <div class="auth-buttons">
          <a href="/login" class="btn">🔒 LOGIN</a>
          <a href="/register" class="btn">➕ REGISTER</a>
        </div>
      </section>
    <% } %>

    <section class="products-section">
      <h2 class="section-title">📖 ПРОДУКТИ</h2>
      <div class="products-grid">
        <% products.forEach(p => { %>
          <div class="product-card">
            <div class="img-wrapper">
              <img src="<%= p.image %>" alt="<%= p.name %>" />
            </div>
            <h3><%= p.name %></h3>
            <p class="desc"><%= p.description %></p>
            <p class="price"><%= p.price.toFixed(2) %> лв.</p>
            <% if (user) { %>
              <form action="/buy/<%= p.id %>" method="POST" class="buy-form">
                <input type="number" name="quantity" value="1" min="1" />
                <button type="submit">🛒 Купи</button>
              </form>
            <% } else { %>
              <button class="btn disabled" onclick="alert('Моля влезте, за да купувате')">🔒</button>
            <% } %>
          </div>
        <% }) %>
      </div>
    </section>

    <section class="hero-section">
      <img src="https://i.postimg.cc/zX8YT9sL/banner.png" alt="banner" class="hero-banner"/>
      <div class="hero-features">
        <span>🔒 ANONYMOUS</span>
        <span>⚡ НЕЗАБАВНО</span>
        <span>💀 UNTRACEABLE</span>
        <span>🌐 24/7 ONLINE</span>
      </div>
    </section>
  </main>

  <!-- FOOTER -->
  <footer class="site-footer">
    <div class="container">
      <p>© 2025 TUMNATAMREJA | ANONYMOUS • SECURE • UNTRACEABLE</p>
    </div>
  </footer>

  <!-- SCRIPTS (matrix + glitch) -->
  <script>
    // … (същия matrix + glitch код от преди) …
  </script>
</body>
</html>
