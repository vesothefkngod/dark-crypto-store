<!DOCTYPE html>

<html lang="bg">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🎉 Успешно плащане - CryptoStore</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🎉</text></svg>">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

```
    body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        color: #fff;
        overflow-x: hidden;
    }

    .success-container {
        background: rgba(255,255,255,0.05);
        backdrop-filter: blur(15px);
        border: 1px solid rgba(255,255,255,0.2);
        border-radius: 25px;
        padding: 3rem;
        width: 100%;
        max-width: 600px;
        box-shadow: 0 20px 40px rgba(0,0,0,0.3);
        text-align: center;
        position: relative;
        overflow: hidden;
    }

    .success-container::before {
        content: '';
        position: absolute;
        top: -50%;
        left: -50%;
        width: 200%;
        height: 200%;
        background: conic-gradient(from 0deg, transparent, rgba(40,167,69,0.1), transparent, rgba(255,107,53,0.1), transparent);
        animation: rotate 10s linear infinite;
        z-index: -1;
    }

    @keyframes rotate {
        to { transform: rotate(360deg); }
    }

    .success-icon {
        font-size: 5rem;
        margin-bottom: 1rem;
        animation: bounce 2s ease-in-out infinite;
    }

    @keyframes bounce {
        0%, 20%, 50%, 80%, 100% {
            transform: translateY(0);
        }
        40% {
            transform: translateY(-20px);
        }
        60% {
            transform: translateY(-10px);
        }
    }

    .success-title {
        font-size: 2.5rem;
        font-weight: 800;
        background: linear-gradient(135deg, #28a745, #20c997);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        margin-bottom: 1rem;
    }

    .success-message {
        font-size: 1.2rem;
        color: rgba(255,255,255,0.8);
        margin-bottom: 2rem;
        line-height: 1.6;
    }

    .order-details {
        background: rgba(40,167,69,0.1);
        border: 2px solid rgba(40,167,69,0.3);
        border-radius: 15px;
        padding: 2rem;
        margin-bottom: 2rem;
        text-align: left;
    }

    .order-details h3 {
        color: #28a745;
        font-size: 1.3rem;
        margin-bottom: 1rem;
        text-align: center;
    }

    .detail-row {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 0.8rem 0;
        border-bottom: 1px solid rgba(255,255,255,0.1);
    }

    .detail-row:last-child {
        border-bottom: none;
    }

    .detail-label {
        color: rgba(255,255,255,0.7);
        font-weight: 600;
    }

    .detail-value {
        color: #fff;
        font-weight: 700;
    }

    .actions {
        display: flex;
        gap: 1rem;
        justify-content: center;
        flex-wrap: wrap;
        margin-bottom: 2rem;
    }

    .btn {
        background: linear-gradient(135deg, #ff6b35, #f7931e);
        color: white;
        padding: 1rem 2rem;
        border: none;
        border-radius: 15px;
        font-size: 1rem;
        font-weight: 700;
        cursor: pointer;
        transition: all 0.3s ease;
        text-decoration: none;
        display: inline-block;
    }

    .btn:hover {
        transform: translateY(-3px);
        box-shadow: 0 15px 35px rgba(255,107,53,0.4);
    }

    .btn-secondary {
        background: linear-gradient(135deg, #6c757d, #495057);
    }

    .btn-secondary:hover {
        box-shadow: 0 15px 35px rgba(108,117,125,0.4);
    }

    .features {
        margin-top: 2rem;
        padding-top: 2rem;
        border-top: 1px solid rgba(255,255,255,0.1);
    }

    .feature-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 1.5rem;
        margin-top: 1rem;
    }

    .feature-card {
        background: rgba(255,255,255,0.03);
        border: 1px solid rgba(255,255,255,0.1);
        border-radius: 15px;
        padding: 1.5rem;
        transition: all 0.3s ease;
    }

    .feature-card:hover {
        background: rgba(255,107,53,0.1);
        border-color: rgba(255,107,53,0.3);
        transform: translateY(-5px);
    }

    .feature-icon {
        font-size: 2.5rem;
        margin-bottom: 1rem;
    }

    .feature-title {
        font-weight: 700;
        margin-bottom: 0.5rem;
        color: #fff;
    }

    .feature-desc {
        color: rgba(255,255,255,0.7);
        font-size: 0.9rem;
    }

    .confetti {
        position: fixed;
        width: 10px;
        height: 10px;
        background: #ff6b35;
        z-index: 1000;
        pointer-events: none;
    }

    .timer {
        background: rgba(255,193,7,0.1);
        border: 1px solid rgba(255,193,7,0.3);
        border-radius: 10px;
        padding: 1rem;
        margin-top: 2rem;
        color: #ffc107;
        font-weight: 600;
    }

    .footer-note {
        margin-top: 2rem;
        color: rgba(255,255,255,0.6);
        font-size: 0.9rem;
        line-height: 1.5;
    }

    @media (max-width: 768px) {
        .success-container {
            margin: 1rem;
            padding: 2rem;
        }
        
        .success-title {
            font-size: 2rem;
        }
        
        .actions {
            flex-direction: column;
        }
        
        .btn {
            width: 100%;
        }
        
        .feature-grid {
            grid-template-columns: 1fr;
        }
    }

    /* Floating particles */
    .particle {
        position: absolute;
        border-radius: 50%;
        pointer-events: none;
    }

    .particle:nth-child(1) {
        width: 6px;
        height: 6px;
        background: #28a745;
        top: 20%;
        left: 10%;
        animation: float 6s ease-in-out infinite;
    }

    .particle:nth-child(2) {
        width: 8px;
        height: 8px;
        background: #ff6b35;
        top: 60%;
        right: 15%;
        animation: float 8s ease-in-out infinite reverse;
    }

    .particle:nth-child(3) {
        width: 4px;
        height: 4px;
        background: #20c997;
        bottom: 30%;
        left: 20%;
        animation: float 10s ease-in-out infinite;
    }

    @keyframes float {
        0%, 100% { transform: translateY(0px) rotate(0deg); }
        33% { transform: translateY(-20px) rotate(120deg); }
        66% { transform: translateY(20px) rotate(240deg); }
    }
</style>
```

</head>
<body>
    <div class="success-container">
        <div class="particle"></div>
        <div class="particle"></div>
        <div class="particle"></div>

```
    <div class="success-icon">🎉</div>
    <h1 class="success-title">Плащането е успешно!</h1>
    <p class="success-message">
        Благодарим ви за покупката! Вашето криптовалутно плащане е обработено успешно.
    </p>

    <% if (order) { %>
        <div class="order-details">
            <h3>📦 Детайли на поръчката</h3>
            <div class="detail-row">
                <span class="detail-label">🆔 Номер на поръчка:</span>
                <span class="detail-value">#<%= order.id %></span>
            </div>
            <div class="detail-row">
                <span class="detail-label">📦 Продукт:</span>
                <span class="detail-value"><%= order.product_name %></span>
            </div>
            <div class="detail-row">
                <span class="detail-label">📊 Количество:</span>
                <span class="detail-value"><%= order.quantity %></span>
            </div>
            <div class="detail-row">
                <span class="detail-label">💰 Обща сума:</span>
                <span class="detail-value">$<%= order.total_amount.toFixed(2) %></span>
            </div>
            <div class="detail-row">
                <span class="detail-label">🏦 Платежен метод:</span>
                <span class="detail-value">
                    <%= order.payment_provider ? order.payment_provider.toUpperCase() : 'Crypto' %>
                    <% if (order.crypto_currency) { %>
                        (<%= order.crypto_currency %>)
                    <% } %>
                </span>
            </div>
            <div class="detail-row">
                <span class="detail-label">👤 Клиент:</span>
                <span class="detail-value"><%= order.username %></span>
            </div>
            <div class="detail-row">
                <span class="detail-label">📅 Дата:</span>
                <span class="detail-value"><%= new Date(order.created_at).toLocaleString('bg-BG') %></span>
            </div>
        </div>
    <% } else { %>
        <div class="order-details">
            <h3>✅ Плащането е потвърдено</h3>
            <p style="text-align: center; color: rgba(255,255,255,0.8);">
                Вашето криптовалутно плащане е успешно обработено и потвърдено в блокчейна.
            </p>
        </div>
    <% } %>

    <div class="actions">
        <a href="/dashboard" class="btn">
            📊 Към Dashboard
        </a>
        <a href="/" class="btn btn-secondary">
            🏠 Към магазина
        </a>
    </div>

    <div class="timer">
        ⏰ Ще бъдете пренасочени автоматично след <span id="countdown">10</span> секунди
    </div>

    <div class="features">
        <h3 style="color: rgba(255,255,255,0.9); margin-bottom: 1rem;">
            🎯 Какво следва?
        </h3>
        <div class="feature-grid">
            <div class="feature-card">
                <div class="feature-icon">📧</div>
                <div class="feature-title">Потвърждение</div>
                <div class="feature-desc">Ще получите email потвърждение с детайлите</div>
            </div>
            <div class="feature-card">
                <div class="feature-icon">📦</div>
                <div class="feature-title">Проследяване</div>
                <div class="feature-desc">Следете статуса в Dashboard секцията</div>
            </div>
            <div class="feature-card">
                <div class="feature-icon">🎁</div>
                <div class="feature-title">Доставка</div>
                <div class="feature-desc">Продуктът ще бъде доставен според условията</div>
            </div>
        </div>
    </div>

    <div class="footer-note">
        <p>
            <strong>💎 Благодарим за доверието!</strong><br>
            За въпроси и поддръжка: <strong>support@cryptostore.com</strong><br>
            Всички плащания са защитени с най-високи стандарти за сигурност.
        </p>
    </div>
</div>

<script>
    // Countdown timer for redirect
    let countdown = 10;
    const countdownElement = document.getElementById('countdown');
    
    const timer = setInterval(() => {
        countdown--;
        if (countdownElement) {
            countdownElement.textContent = countdown;
        }
        
        if (countdown <= 0) {
            clearInterval(timer);
            window.location.href = '/dashboard';
        }
    }, 1000);

    // Confetti effect
    function createConfetti() {
        const colors = ['#ff6b35', '#f7931e', '#28a745', '#20c997', '#007bff'];
        
        for (let i = 0; i < 50; i++) {
            setTimeout(() => {
                const confetti = document.createElement('div');
                confetti.className = 'confetti';
                confetti.style.left = Math.random() * 100 + '%';
                confetti.style.backgroundColor = colors[Math.floor(Math.random() * colors.length)];
                confetti.style.animationDuration = (Math.random() * 3 + 2) + 's';
                
                document.body.appendChild(confetti);
                
                confetti.animate([
                    { transform: 'translateY(-100vh) rotate(0deg)', opacity: 1 },
                    { transform: 'translateY(100vh) rotate(360deg)', opacity: 0 }
                ], {
                    duration: 3000,
                    easing: 'cubic-bezier(0.25, 0.46, 0.45, 0.94)'
                });
                
                setTimeout(() => confetti.remove(), 3000);
            }, i * 100);
        }
    }

    // Trigger confetti on load
    window.addEventListener('load', () => {
        setTimeout(createConfetti, 500);
    });

    // Success sound effect (if supported)
    function playSuccessSound() {
        try {
            const audioContext = new (window.AudioContext || window.webkitAudioContext)();
            const oscillator = audioContext.createOscillator();
            const gainNode = audioContext.createGain();
            
            oscillator.connect(gainNode);
            gainNode.connect(audioContext.destination);
            
            oscillator.frequency.setValueAtTime(800, audioContext.currentTime);
            oscillator.frequency.setValueAtTime(1000, audioContext.currentTime + 0.1);
            oscillator.frequency.setValueAtTime(1200, audioContext.currentTime + 0.2);
            
            gainNode.gain.setValueAtTime(0.3, audioContext.currentTime);
            gainNode.gain.exponentialRampToValueAtTime(0.01, audioContext.currentTime + 0.5);
            
            oscillator.start(audioContext.currentTime);
            oscillator.stop(audioContext.currentTime + 0.5);
        } catch (e) {
            // Silence audio errors
        }
    }

    // Play sound on load
    document.addEventListener('click', playSuccessSound, { once: true });

    // Add some interactive effects
    document.querySelectorAll('.feature-card').forEach(card => {
        card.addEventListener('mouseenter', () => {
            card.style.transform = 'translateY(-5px) scale(1.02)';
        });
        
        card.addEventListener('mouseleave', () => {
            card.style.transform = 'translateY(0) scale(1)';
        });
    });

    // Keyboard shortcuts
    document.addEventListener('keydown', (e) => {
        if (e.key === 'd' || e.key === 'D') {
            window.location.href = '/dashboard';
        } else if (e.key === 'h' || e.key === 'H') {
            window.location.href = '/';
        } else if (e.key === 'Escape') {
            clearInterval(timer);
            window.location.href = '/dashboard';
        }
    });

    // Show keyboard shortcuts hint
    setTimeout(() => {
        const hint = document.createElement('div');
        hint.style.cssText = `
            position: fixed;
            bottom: 20px;
            left: 50%;
            transform: translateX(-50%);
            background: rgba(0,0,0,0.8);
            color: white;
            padding: 10px 20px;
            border-radius: 25px;
            font-size: 0.8rem;
            z-index: 1001;
            animation: fadeIn 0.5s ease;
        `;
        hint.textContent = 'Shortcuts: D = Dashboard, H = Home, Esc = Skip timer';
        document.body.appendChild(hint);
        
        setTimeout(() => hint.remove(), 3000);
    }, 2000);
</script>
```

</body>
</html>
